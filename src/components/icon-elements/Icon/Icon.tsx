import React from 'react';

export interface IconProps {
    size: string,
    bgColor: string,
    cornerRadius: string,
}

const Icon = () => {
    return(
        <></>
    );
};

export default Icon;
