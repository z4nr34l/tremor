import React from 'react';

import { classNames } from '@utils/classname-utils';

export interface IconProps {
    size: string,
    bgColor: string,
    cornerRadius: string,
}

const Icon = () => {
    return(
        <></>
    );
};

export default Icon;
